#!/usr/bin/env bash
# simsc_agents wrapper for python_agent.agents.p_agent_v4 (Protoss).
# See simsc_agents/README.md for the launch contract this honours.
set -e
cd "$(dirname "$0")/.."
exec python3 -m python_agent.agents.p_agent_v4 "$@"
