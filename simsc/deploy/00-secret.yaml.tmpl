apiVersion: v1
kind: Secret
metadata:
  name: simsc-app-env
  namespace: ${K8S_NAMESPACE}
type: Opaque
data:
  # Base64-encoded. Values here become container env vars via envFrom.
  ADMIN_TOKEN: ${ADMIN_TOKEN_BASE64}
